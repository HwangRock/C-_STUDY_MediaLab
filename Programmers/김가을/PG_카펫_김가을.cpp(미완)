#include <vector>
#include <string>
#include <algorithm>
#include <iostream>

int main(int argc, char const* argv[])
{

    int brown, yellow, w = 0, h = 0;
    std::vector<int> answer;
    answer.push_back(w);
    answer.push_back(h);
    std::cout << answer[0] << answer[1];

    

    return 0;
}
