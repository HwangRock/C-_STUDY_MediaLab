#define _CRT_SECURE_NO_WARNINGS
#include <vector>
#include <string>
#include <algorithm>
#include <iostream>

#include <queue>
#include <map>
#include <stdio.h>

using namespace std;


int main(int argc, char const* argv[])
{
   // 백준 가장 긴 증가하는 부분 수열
    int n, l;
    vector<int> sequence;
    

    scanf("%d", &n);

    for (int i = 0; i < n; i++) {
        int a;
        scanf("%d", &a);
        sequence.push_back(a);
    }
    
    sort(sequence.begin(), sequence.end());
    
    for (int i = 0; i < n; i++) {
        for (int j = i; j < n; j++) {
            if (sequence[i] == sequence[j]) sequence.erase(sequence.begin() + j);
        }
    }

    for (auto a : sequence) {
        cout << sequence[a] << " ";
    }


    //l = sequence.size();
   // printf("%d", l);

    return 0;
}



